<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.customlistview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListarCarros" />
        <activity android:name=".RegistrarCarros" />
        <activity android:name=".Reportes" />
        <activity android:name=".CarrosRegistrados" />
        <activity android:name=".PorMarca" />
        <activity android:name=".PorColor"></activity>
    </application>

</manifest>